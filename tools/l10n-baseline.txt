# Nợ localization của template — danh sách này chỉ được CO LẠI.
#
# Hiện tại: **trống**. Mọi chuỗi user-facing trong Features/ và DesignSystem/ đã đi qua
# bảng dịch, nên đổi ngôn ngữ trong app là cả màn hình đổi theo — đó cũng là cách duy
# nhất để nhìn thấy cơ chế có chạy hay không.
#
# Nếu phải thêm một dòng vào đây: đó là một chuỗi hardcode mới, và nó chỉ được phép khi
# có lý do viết ra. Thêm dòng là nở nợ, và script không chặn được điều đó — chỉ người
# review chặn được.
#
# Chuỗi trong Data/ và accessibilityLabel vẫn ngoài phạm vi script — xem skill `ios-l10n`.
